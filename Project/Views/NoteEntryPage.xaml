<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Project.Views.NoteEntryPage"
             Title="Edit Ticket">
    <ContentPage.Resources>
        <!-- Implicit styles -->
        <Style TargetType="{x:Type Editor}">
            <Setter Property="BackgroundColor"
                    Value="{StaticResource AppBackgroundColor}" />
        </Style>
    </ContentPage.Resources>

    <!-- Layout children vertically -->
    <StackLayout Margin="{StaticResource PageMargin}">
        <Label Text="Clients Name:"
                           FontSize="Medium" />
        <Editor Placeholder="Enter the client's name"
                Text="{Binding Text}"
                HeightRequest="50" />
        <Label Text="Due date (mm/dd/yyyy):"
                           FontSize="Medium" />
        <Editor Placeholder="Enter the date due by"
                Text="{Binding Date, StringFormat='{0:MM/dd/yyyy}'}"
                HeightRequest="50" />
        <Label Text="Time taken:"
                           FontSize="Medium" />
        <Editor Placeholder="Enter the time taken"
                Text="{Binding Time}"
                HeightRequest="50" />
        <Label Text="Comments:"
                           FontSize="Medium" />
        <Editor Placeholder="Comments"
                Text="{Binding Comment}"
                HeightRequest="100" />
        <Grid ColumnDefinitions="*,*">
            <Label Grid.Row="0" Text="Completed:"
                           FontSize="Medium" />
            <CheckBox Grid.Row="0" Grid.Column="1" IsChecked="{Binding isChecked}"  x:Name="checkBox"  Color="#1976D2" />
            <BoxView Grid.Row="1" 
                     HeightRequest="1" Color="White" Margin="0, 0, 0 ,0"/>
        </Grid>
        <Grid ColumnDefinitions="*,*">
            <!-- Layout children in two columns -->
            <Button Text="Save"
                    Clicked="OnSaveButtonClicked" />
            <Button Grid.Column="1"
                    Text="Delete"
                    Clicked="OnDeleteButtonClicked"/>
        </Grid>
    </StackLayout>
</ContentPage>